public class Solution {
    public static int lowerBound(int []arr, int n, int x) {
        int ans = arr.length;
        for (int i=0;i<arr.length;i++){
            if (arr[i]>=x){
                return i;
            }
        }
        return n;
    }
}
